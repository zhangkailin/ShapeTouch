/*! ShapeTouch v1.0.0 | (c) AiSouNet | GNU General Public License Version 3 | https://github.com/zhangkailin/ShapeTouch*/
(function(king){console.log("https://github.com/zhangkailin/ShapeTouch");king.shape=king.shape||{};let shape=king.shape;shape.touch=function(src,x,y,callback){let img=new Image();img.onload=function(){let canvas=document.createElement("Canvas");let ctx=canvas.getContext("2d");ctx.drawImage(img,0,0);let imgData=ctx.getImageData(x,img.height-y,1,1);let alpha=imgData.data[3];if(callback)callback({touch:alpha>0,alpha:alpha})};img.src=src};shape.touchPoint=function(src,point,callback){shape.touch(src,point.x,point.y,callback)}}(window.king=window.king||{}));